{
  "description": "Default commit gate: reviews staged changes when you commit through Claude Code and blocks confident high-severity findings. Fails open. For every-commit-everywhere coverage, also run bin/install-git-hook.sh.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/bin/gate-hook.sh\"",
            "if": "Bash(git commit:*)",
            "timeout": 300,
            "statusMessage": "Reviewing staged changes (subscription-code-review)..."
          }
        ]
      }
    ]
  }
}
