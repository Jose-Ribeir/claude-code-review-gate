{
  "description": "Manual-only: auto-commit gate removed (cost — 1 reviewer subagent per file was spawned on every commit). Run /review-gate:review manually before pushing. For every-commit-everywhere coverage via the git hook instead, run bin/install-git-hook.sh.",
  "hooks": {}
}
