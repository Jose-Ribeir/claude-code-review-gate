{
  "description": "Pre-push gate: reviews all unpushed commits once when you push through Claude Code. Fires once per push (not per commit), covering the cumulative diff of all commits being pushed. Blocks confident high-severity findings. Fails open. For every-push-everywhere coverage outside Claude Code, run bin/install-git-hook.sh.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/bin/gate-hook.sh\"",
            "if": "Bash(git push:*)",
            "timeout": 1920,
            "statusMessage": "Reviewing unpushed commits (review-gate)..."
          }
        ]
      }
    ]
  }
}
